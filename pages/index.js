import React from 'react';
import Products from '../components/Products';

// import useRouter from "routre/"

const index = () => (
  // const { query } = useRouter();
  // const page = parseInt(query.page);
  <div>
    <Products />
  </div>
);
export default index;
